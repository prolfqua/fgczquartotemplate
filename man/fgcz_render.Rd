% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assets.R
\name{fgcz_render}
\alias{fgcz_render}
\title{Render an FGCZ Quarto report}
\usage{
fgcz_render(input, buttons = FALSE, ...)
}
\arguments{
\item{input}{Path to the \code{.qmd} to render.}

\item{buttons}{Which top-right toolbar buttons (\code{fgcz-plot-finder.html}) to
add. Defaults to \code{FALSE}; \code{TRUE} enables both buttons for backward
compatibility. Alternatively, pass \code{"search"} for Find, \code{"download"} for
Download, or \code{c("search", "download")} for both. \code{FALSE}, \code{NULL}, and
\code{character(0)} disable the toolbar.}

\item{...}{Passed on to \code{\link[quarto:quarto_render]{quarto::quarto_render()}} (e.g. \code{execute_params},
\code{output_file}, \code{quarto_args}). A \code{metadata} list passed here is honored;
enabling buttons merges \code{include-after-body} into it.}
}
\value{
The value of \code{\link[quarto:quarto_render]{quarto::quarto_render()}}, invisibly.
}
\description{
Stages the shared styling assets next to \code{input} (via \code{\link[=fgcz_copy_assets]{fgcz_copy_assets()}})
and then renders it with \code{\link[quarto:quarto_render]{quarto::quarto_render()}}. The \code{.qmd} needs no
styling front matter at all: the staged \verb{_metadata.yml} is picked up by
Quarto automatically, so the report stays fully portable.
}
\examples{
\dontrun{
fgcz_render("CountQC.qmd", execute_params = list(reportTitle = "CountQC"))
fgcz_render("CountQC.qmd", buttons = TRUE) # with the Find/Download toolbar
fgcz_render("CountQC.qmd", buttons = "search") # Find only
}
}
