name: altdoc

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  altdoc:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6.0.3

      - uses: quarto-dev/quarto-actions/setup@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::altdoc
            any::rmarkdown
            any::knitr
            any::DT
            any::ggplot2
            any::plotly
          needs: website

      - name: Verify vignette extension mirror is in sync
        run: |
          make sync
          if [ -n "$(git status --porcelain -- vignettes/_extensions)" ]; then
            echo "::error::vignettes/_extensions is out of date — run 'make sync' and commit"
            git --no-pager diff -- vignettes/_extensions
            exit 1
          fi

      - name: Install package
        run: R CMD INSTALL .

      - name: Build docs with altdoc
        run: Rscript -e 'altdoc::render_docs(freeze = FALSE)'

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: docs
          clean: true
